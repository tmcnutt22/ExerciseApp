# ExerciseApp

Exercise app is a CRUD app to track exercises that you have performed.

To use this app, clone the repository and in both the client and server directories run:
npm install
npm start

The app will run on localhost:8000
